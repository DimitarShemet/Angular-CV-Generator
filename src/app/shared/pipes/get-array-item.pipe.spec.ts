import { GetArrayItemPipe } from './get-array-item.pipe';

describe('GetArrayItemPipe', () => {
  it('create an instance', () => {
    const pipe = new GetArrayItemPipe();
    expect(pipe).toBeTruthy();
  });
});
